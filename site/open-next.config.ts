import { defineCloudflareConfig } from '@opennextjs/cloudflare'

/**
 * Пустая конфигурация — намеренно.
 *
 * Кэш инкрементальной регенерации по умолчанию живёт в памяти воркера и
 * не переживает его перезапуск. Для показа заказчику это незаметно:
 * страницы всё равно пересобираются раз в 60 секунд, а промах кэша стоит
 * одного запроса в Sanity.
 *
 * Когда сайт пойдёт в бой, сюда добавляется r2IncrementalCache — тогда
 * пересобранная страница переживает перезапуск и не пересчитывается заново
 * на каждом холодном старте. Шаг описан в DEPLOY.md.
 */
export default defineCloudflareConfig({})
